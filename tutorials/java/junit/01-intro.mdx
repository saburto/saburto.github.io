---
draft: true
---
# 01 - Introduction

The goal of this tutorial is to be profency in the use of Junit5.


## Requeriments

- Console Terminal using **bash/zsh** or any other similar.
- Any **text editor**. There is no necessary any IDE.
- Basic knowledge of **Java**.
- Java **JDK 21** or superior.


<details>
<summary>How to install JDK</summary>

Using **sdkman**:

  ```bash
  sdk install java
  ```

:::tip
  Verify the current version of java:
  ```bash
  java -version # --version
  ```
  It should return something like this, it depends on your OS and architecture.

  ```
openjdk 21.0.5 2024-10-15 LTS
OpenJDK Runtime Environment Temurin-21.0.5+11 (build 21.0.5+11-LTS)
OpenJDK 64-Bit Server VM Temurin-21.0.5+11 (build 21.0.5+11-LTS, mixed mode, sharing)
  ```
:::
</details>

- Maven. 


<details>
<summary>How to install Maven</summary>

Using **sdkman**:

  ```bash
  sdk install maven
  ```

:::tip

Verify the current version of Maven:

```bash
mvn -version
```

Result looks like this:

```bash
Apache Maven 3.9.9 (8e8579a9e76f7d015ee5ec7bfcdc97d260186937)
Maven home: /home/saburto/.sdkman/candidates/maven/current
Java version: 21.0.5, vendor: Eclipse Adoptium, runtime: /home/saburto/.sdkman/candidates/java/21.0.5-tem
Default locale: en_US, platform encoding: UTF-8
OS name: "linux", version: "6.8.0-49-generic", arch: "amd64", family: "unix"
```

:::

</details>


## Advice

Try to not copy and paste the source code or the commands, try by your self step-by-step.


## Test your self


import React, {useState} from 'react';

export const Quiz = () => {

  const [result, setResult] = useState("")
  
  const handleAnswer = (event) => {
    if (event.target.value === "java -version") {
        setResult("Correct!")
      } else {
          setResult("No correcto!")
        }

    };

  return (<>
          <p> How to verify the current JDK version? </p>
          <input type="textarea" onChange={handleAnswer} />
          {result}
          <p> How to verify the current Maven version? </p>
          <input type="textarea" />
          </>);

}

<Quiz/>





